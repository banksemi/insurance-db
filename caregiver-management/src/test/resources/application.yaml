spring:
  datasource:
    url: jdbc:h2:mem:test
    driver-class-name: org.h2.Driver

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect # url에서 h2를 유추할 수는 있지만 그래도 써주자.
    hibernate:
      ddl-auto: create-drop
    properties:
      format_sql: true
